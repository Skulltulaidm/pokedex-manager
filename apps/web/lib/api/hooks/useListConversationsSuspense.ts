/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type { ListConversationsQueryResponse } from "../types/ListConversations"
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@kubb/plugin-client/clients/axios"
import type {
  QueryKey,
  QueryClient,
  UseSuspenseQueryOptions,
  UseSuspenseQueryResult,
} from "@tanstack/react-query"
import { listConversations } from "../clients/listConversations"
import { queryOptions, useSuspenseQuery } from "@tanstack/react-query"

export const listConversationsSuspenseQueryKey = () =>
  [{ url: "/api/v1/chat" }] as const

export type ListConversationsSuspenseQueryKey = ReturnType<
  typeof listConversationsSuspenseQueryKey
>

export function listConversationsSuspenseQueryOptions(
  config: Partial<RequestConfig> & { client?: Client } = {}
) {
  const queryKey = listConversationsSuspenseQueryKey()
  return queryOptions<
    ListConversationsQueryResponse,
    ResponseErrorConfig<Error>,
    ListConversationsQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return listConversations({ ...config, signal: config.signal ?? signal })
    },
  })
}

/**
 * @summary List Conversations
 * {@link /api/v1/chat}
 */
export function useListConversationsSuspense<
  TData = ListConversationsQueryResponse,
  TQueryKey extends QueryKey = ListConversationsSuspenseQueryKey,
>(
  options: {
    query?: Partial<
      UseSuspenseQueryOptions<
        ListConversationsQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryKey
      >
    > & { client?: QueryClient }
    client?: Partial<RequestConfig> & { client?: Client }
  } = {}
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {}
  const { client: queryClient, ...resolvedOptions } = queryConfig
  const queryKey =
    resolvedOptions?.queryKey ?? listConversationsSuspenseQueryKey()

  const query = useSuspenseQuery(
    {
      ...listConversationsSuspenseQueryOptions(config),
      ...resolvedOptions,
      queryKey,
    } as unknown as UseSuspenseQueryOptions,
    queryClient
  ) as UseSuspenseQueryResult<TData, ResponseErrorConfig<Error>> & {
    queryKey: TQueryKey
  }

  query.queryKey = queryKey as TQueryKey

  return query
}
