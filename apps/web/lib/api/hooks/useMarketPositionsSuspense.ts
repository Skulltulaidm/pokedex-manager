/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  MarketPositionsQueryResponse,
  MarketPositionsQueryParams,
  MarketPositions422,
} from "../types/MarketPositions"
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@kubb/plugin-client/clients/axios"
import type {
  QueryKey,
  QueryClient,
  UseSuspenseQueryOptions,
  UseSuspenseQueryResult,
} from "@tanstack/react-query"
import { marketPositions } from "../clients/marketPositions"
import { queryOptions, useSuspenseQuery } from "@tanstack/react-query"

export const marketPositionsSuspenseQueryKey = (
  params?: MarketPositionsQueryParams
) =>
  [
    { url: "/api/v1/catalog/market/positions" },
    ...(params ? [params] : []),
  ] as const

export type MarketPositionsSuspenseQueryKey = ReturnType<
  typeof marketPositionsSuspenseQueryKey
>

export function marketPositionsSuspenseQueryOptions(
  params?: MarketPositionsQueryParams,
  config: Partial<RequestConfig> & { client?: Client } = {}
) {
  const queryKey = marketPositionsSuspenseQueryKey(params)
  return queryOptions<
    MarketPositionsQueryResponse,
    ResponseErrorConfig<MarketPositions422>,
    MarketPositionsQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return marketPositions(params, {
        ...config,
        signal: config.signal ?? signal,
      })
    },
  })
}

/**
 * @description Every held card as a position: cost, value, gain and weight in the portfolio.
 * @summary Market Positions
 * {@link /api/v1/catalog/market/positions}
 */
export function useMarketPositionsSuspense<
  TData = MarketPositionsQueryResponse,
  TQueryKey extends QueryKey = MarketPositionsSuspenseQueryKey,
>(
  params?: MarketPositionsQueryParams,
  options: {
    query?: Partial<
      UseSuspenseQueryOptions<
        MarketPositionsQueryResponse,
        ResponseErrorConfig<MarketPositions422>,
        TData,
        TQueryKey
      >
    > & { client?: QueryClient }
    client?: Partial<RequestConfig> & { client?: Client }
  } = {}
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {}
  const { client: queryClient, ...resolvedOptions } = queryConfig
  const queryKey =
    resolvedOptions?.queryKey ?? marketPositionsSuspenseQueryKey(params)

  const query = useSuspenseQuery(
    {
      ...marketPositionsSuspenseQueryOptions(params, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as UseSuspenseQueryOptions,
    queryClient
  ) as UseSuspenseQueryResult<
    TData,
    ResponseErrorConfig<MarketPositions422>
  > & { queryKey: TQueryKey }

  query.queryKey = queryKey as TQueryKey

  return query
}
