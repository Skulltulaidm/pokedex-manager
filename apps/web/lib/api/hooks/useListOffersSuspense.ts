/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  ListOffersQueryResponse,
  ListOffersQueryParams,
  ListOffers422,
} from "../types/ListOffers"
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@kubb/plugin-client/clients/axios"
import type {
  QueryKey,
  QueryClient,
  UseSuspenseQueryOptions,
  UseSuspenseQueryResult,
} from "@tanstack/react-query"
import { listOffers } from "../clients/listOffers"
import { queryOptions, useSuspenseQuery } from "@tanstack/react-query"

export const listOffersSuspenseQueryKey = (params?: ListOffersQueryParams) =>
  [{ url: "/api/v1/trades/offers" }, ...(params ? [params] : [])] as const

export type ListOffersSuspenseQueryKey = ReturnType<
  typeof listOffersSuspenseQueryKey
>

export function listOffersSuspenseQueryOptions(
  params?: ListOffersQueryParams,
  config: Partial<RequestConfig> & { client?: Client } = {}
) {
  const queryKey = listOffersSuspenseQueryKey(params)
  return queryOptions<
    ListOffersQueryResponse,
    ResponseErrorConfig<ListOffers422>,
    ListOffersQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return listOffers(params, { ...config, signal: config.signal ?? signal })
    },
  })
}

/**
 * @description Offers the reader is party to, sent and received, newest first.
 * @summary List Offers
 * {@link /api/v1/trades/offers}
 */
export function useListOffersSuspense<
  TData = ListOffersQueryResponse,
  TQueryKey extends QueryKey = ListOffersSuspenseQueryKey,
>(
  params?: ListOffersQueryParams,
  options: {
    query?: Partial<
      UseSuspenseQueryOptions<
        ListOffersQueryResponse,
        ResponseErrorConfig<ListOffers422>,
        TData,
        TQueryKey
      >
    > & { client?: QueryClient }
    client?: Partial<RequestConfig> & { client?: Client }
  } = {}
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {}
  const { client: queryClient, ...resolvedOptions } = queryConfig
  const queryKey =
    resolvedOptions?.queryKey ?? listOffersSuspenseQueryKey(params)

  const query = useSuspenseQuery(
    {
      ...listOffersSuspenseQueryOptions(params, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as UseSuspenseQueryOptions,
    queryClient
  ) as UseSuspenseQueryResult<TData, ResponseErrorConfig<ListOffers422>> & {
    queryKey: TQueryKey
  }

  query.queryKey = queryKey as TQueryKey

  return query
}
