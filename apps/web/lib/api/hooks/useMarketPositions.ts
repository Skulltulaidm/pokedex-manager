/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  MarketPositionsQueryResponse,
  MarketPositionsQueryParams,
  MarketPositions422,
} from "../types/MarketPositions"
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@kubb/plugin-client/clients/axios"
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query"
import { marketPositions } from "../clients/marketPositions"
import { queryOptions, useQuery } from "@tanstack/react-query"

export const marketPositionsQueryKey = (params?: MarketPositionsQueryParams) =>
  [
    { url: "/api/v1/catalog/market/positions" },
    ...(params ? [params] : []),
  ] as const

export type MarketPositionsQueryKey = ReturnType<typeof marketPositionsQueryKey>

export function marketPositionsQueryOptions(
  params?: MarketPositionsQueryParams,
  config: Partial<RequestConfig> & { client?: Client } = {}
) {
  const queryKey = marketPositionsQueryKey(params)
  return queryOptions<
    MarketPositionsQueryResponse,
    ResponseErrorConfig<MarketPositions422>,
    MarketPositionsQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return marketPositions(params, {
        ...config,
        signal: config.signal ?? signal,
      })
    },
  })
}

/**
 * @description Every held card as a position: cost, value, gain and weight in the portfolio.
 * @summary Market Positions
 * {@link /api/v1/catalog/market/positions}
 */
export function useMarketPositions<
  TData = MarketPositionsQueryResponse,
  TQueryData = MarketPositionsQueryResponse,
  TQueryKey extends QueryKey = MarketPositionsQueryKey,
>(
  params?: MarketPositionsQueryParams,
  options: {
    query?: Partial<
      QueryObserverOptions<
        MarketPositionsQueryResponse,
        ResponseErrorConfig<MarketPositions422>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient }
    client?: Partial<RequestConfig> & { client?: Client }
  } = {}
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {}
  const { client: queryClient, ...resolvedOptions } = queryConfig
  const queryKey = resolvedOptions?.queryKey ?? marketPositionsQueryKey(params)

  const query = useQuery(
    {
      ...marketPositionsQueryOptions(params, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient
  ) as UseQueryResult<TData, ResponseErrorConfig<MarketPositions422>> & {
    queryKey: TQueryKey
  }

  query.queryKey = queryKey as TQueryKey

  return query
}
