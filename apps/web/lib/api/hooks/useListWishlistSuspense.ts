/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type { ListWishlistQueryResponse } from "../types/ListWishlist"
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@kubb/plugin-client/clients/axios"
import type {
  QueryKey,
  QueryClient,
  UseSuspenseQueryOptions,
  UseSuspenseQueryResult,
} from "@tanstack/react-query"
import { listWishlist } from "../clients/listWishlist"
import { queryOptions, useSuspenseQuery } from "@tanstack/react-query"

export const listWishlistSuspenseQueryKey = () =>
  [{ url: "/api/v1/wishlist" }] as const

export type ListWishlistSuspenseQueryKey = ReturnType<
  typeof listWishlistSuspenseQueryKey
>

export function listWishlistSuspenseQueryOptions(
  config: Partial<RequestConfig> & { client?: Client } = {}
) {
  const queryKey = listWishlistSuspenseQueryKey()
  return queryOptions<
    ListWishlistQueryResponse,
    ResponseErrorConfig<Error>,
    ListWishlistQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return listWishlist({ ...config, signal: config.signal ?? signal })
    },
  })
}

/**
 * @summary List Wishlist
 * {@link /api/v1/wishlist}
 */
export function useListWishlistSuspense<
  TData = ListWishlistQueryResponse,
  TQueryKey extends QueryKey = ListWishlistSuspenseQueryKey,
>(
  options: {
    query?: Partial<
      UseSuspenseQueryOptions<
        ListWishlistQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryKey
      >
    > & { client?: QueryClient }
    client?: Partial<RequestConfig> & { client?: Client }
  } = {}
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {}
  const { client: queryClient, ...resolvedOptions } = queryConfig
  const queryKey = resolvedOptions?.queryKey ?? listWishlistSuspenseQueryKey()

  const query = useSuspenseQuery(
    {
      ...listWishlistSuspenseQueryOptions(config),
      ...resolvedOptions,
      queryKey,
    } as unknown as UseSuspenseQueryOptions,
    queryClient
  ) as UseSuspenseQueryResult<TData, ResponseErrorConfig<Error>> & {
    queryKey: TQueryKey
  }

  query.queryKey = queryKey as TQueryKey

  return query
}
