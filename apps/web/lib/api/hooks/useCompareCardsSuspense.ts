/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  CompareCardsQueryResponse,
  CompareCardsQueryParams,
  CompareCards422,
} from "../types/CompareCards"
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@kubb/plugin-client/clients/axios"
import type {
  QueryKey,
  QueryClient,
  UseSuspenseQueryOptions,
  UseSuspenseQueryResult,
} from "@tanstack/react-query"
import { compareCards } from "../clients/compareCards"
import { queryOptions, useSuspenseQuery } from "@tanstack/react-query"

export const compareCardsSuspenseQueryKey = (params: CompareCardsQueryParams) =>
  [{ url: "/api/v1/catalog/compare" }, ...(params ? [params] : [])] as const

export type CompareCardsSuspenseQueryKey = ReturnType<
  typeof compareCardsSuspenseQueryKey
>

export function compareCardsSuspenseQueryOptions(
  params: CompareCardsQueryParams,
  config: Partial<RequestConfig> & { client?: Client } = {}
) {
  const queryKey = compareCardsSuspenseQueryKey(params)
  return queryOptions<
    CompareCardsQueryResponse,
    ResponseErrorConfig<CompareCards422>,
    CompareCardsQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return compareCards(params, {
        ...config,
        signal: config.signal ?? signal,
      })
    },
  })
}

/**
 * @description What a model sees when the two card images are put side by side.
 * @summary Compare Cards
 * {@link /api/v1/catalog/compare}
 */
export function useCompareCardsSuspense<
  TData = CompareCardsQueryResponse,
  TQueryKey extends QueryKey = CompareCardsSuspenseQueryKey,
>(
  params: CompareCardsQueryParams,
  options: {
    query?: Partial<
      UseSuspenseQueryOptions<
        CompareCardsQueryResponse,
        ResponseErrorConfig<CompareCards422>,
        TData,
        TQueryKey
      >
    > & { client?: QueryClient }
    client?: Partial<RequestConfig> & { client?: Client }
  } = {}
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {}
  const { client: queryClient, ...resolvedOptions } = queryConfig
  const queryKey =
    resolvedOptions?.queryKey ?? compareCardsSuspenseQueryKey(params)

  const query = useSuspenseQuery(
    {
      ...compareCardsSuspenseQueryOptions(params, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as UseSuspenseQueryOptions,
    queryClient
  ) as UseSuspenseQueryResult<TData, ResponseErrorConfig<CompareCards422>> & {
    queryKey: TQueryKey
  }

  query.queryKey = queryKey as TQueryKey

  return query
}
