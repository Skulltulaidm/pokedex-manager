/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  GetThreadWithQueryResponse,
  GetThreadWithPathParams,
  GetThreadWith422,
} from "../types/GetThreadWith"
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@kubb/plugin-client/clients/axios"
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query"
import { getThreadWith } from "../clients/getThreadWith"
import { queryOptions, useQuery } from "@tanstack/react-query"

export const getThreadWithQueryKey = (
  partner_id: GetThreadWithPathParams["partner_id"] | undefined
) =>
  [
    {
      url: "/api/v1/messages/with/:partner_id",
      params: { partner_id: partner_id },
    },
  ] as const

export type GetThreadWithQueryKey = ReturnType<typeof getThreadWithQueryKey>

export function getThreadWithQueryOptions(
  partner_id: GetThreadWithPathParams["partner_id"] | undefined,
  config: Partial<RequestConfig> & { client?: Client } = {}
) {
  const queryKey = getThreadWithQueryKey(partner_id)
  return queryOptions<
    GetThreadWithQueryResponse,
    ResponseErrorConfig<GetThreadWith422>,
    GetThreadWithQueryResponse,
    typeof queryKey
  >({
    enabled: !!partner_id,
    queryKey,
    queryFn: async ({ signal }) => {
      return getThreadWith(partner_id!, {
        ...config,
        signal: config.signal ?? signal,
      })
    },
  })
}

/**
 * @description The conversation with one collector, started or not.
 * @summary Get Thread With
 * {@link /api/v1/messages/with/:partner_id}
 */
export function useGetThreadWith<
  TData = GetThreadWithQueryResponse,
  TQueryData = GetThreadWithQueryResponse,
  TQueryKey extends QueryKey = GetThreadWithQueryKey,
>(
  partner_id: GetThreadWithPathParams["partner_id"] | undefined,
  options: {
    query?: Partial<
      QueryObserverOptions<
        GetThreadWithQueryResponse,
        ResponseErrorConfig<GetThreadWith422>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient }
    client?: Partial<RequestConfig> & { client?: Client }
  } = {}
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {}
  const { client: queryClient, ...resolvedOptions } = queryConfig
  const queryKey =
    resolvedOptions?.queryKey ?? getThreadWithQueryKey(partner_id)

  const query = useQuery(
    {
      ...getThreadWithQueryOptions(partner_id, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient
  ) as UseQueryResult<TData, ResponseErrorConfig<GetThreadWith422>> & {
    queryKey: TQueryKey
  }

  query.queryKey = queryKey as TQueryKey

  return query
}
