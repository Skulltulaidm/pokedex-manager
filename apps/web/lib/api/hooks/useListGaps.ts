/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  ListGapsQueryResponse,
  ListGapsQueryParams,
  ListGaps422,
} from "../types/ListGaps"
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@kubb/plugin-client/clients/axios"
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query"
import { listGaps } from "../clients/listGaps"
import { queryOptions, useQuery } from "@tanstack/react-query"

export const listGapsQueryKey = (params?: ListGapsQueryParams) =>
  [{ url: "/api/v1/gaps" }, ...(params ? [params] : [])] as const

export type ListGapsQueryKey = ReturnType<typeof listGapsQueryKey>

export function listGapsQueryOptions(
  params?: ListGapsQueryParams,
  config: Partial<RequestConfig> & { client?: Client } = {}
) {
  const queryKey = listGapsQueryKey(params)
  return queryOptions<
    ListGapsQueryResponse,
    ResponseErrorConfig<ListGaps422>,
    ListGapsQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return listGaps(params, { ...config, signal: config.signal ?? signal })
    },
  })
}

/**
 * @summary List Gaps
 * {@link /api/v1/gaps}
 */
export function useListGaps<
  TData = ListGapsQueryResponse,
  TQueryData = ListGapsQueryResponse,
  TQueryKey extends QueryKey = ListGapsQueryKey,
>(
  params?: ListGapsQueryParams,
  options: {
    query?: Partial<
      QueryObserverOptions<
        ListGapsQueryResponse,
        ResponseErrorConfig<ListGaps422>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient }
    client?: Partial<RequestConfig> & { client?: Client }
  } = {}
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {}
  const { client: queryClient, ...resolvedOptions } = queryConfig
  const queryKey = resolvedOptions?.queryKey ?? listGapsQueryKey(params)

  const query = useQuery(
    {
      ...listGapsQueryOptions(params, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient
  ) as UseQueryResult<TData, ResponseErrorConfig<ListGaps422>> & {
    queryKey: TQueryKey
  }

  query.queryKey = queryKey as TQueryKey

  return query
}
