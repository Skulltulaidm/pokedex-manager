/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  ListThreadsQueryResponse,
  ListThreadsQueryParams,
  ListThreads422,
} from "../types/ListThreads"
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@kubb/plugin-client/clients/axios"
import type {
  QueryKey,
  QueryClient,
  UseSuspenseQueryOptions,
  UseSuspenseQueryResult,
} from "@tanstack/react-query"
import { listThreads } from "../clients/listThreads"
import { queryOptions, useSuspenseQuery } from "@tanstack/react-query"

export const listThreadsSuspenseQueryKey = (params?: ListThreadsQueryParams) =>
  [{ url: "/api/v1/messages" }, ...(params ? [params] : [])] as const

export type ListThreadsSuspenseQueryKey = ReturnType<
  typeof listThreadsSuspenseQueryKey
>

export function listThreadsSuspenseQueryOptions(
  params?: ListThreadsQueryParams,
  config: Partial<RequestConfig> & { client?: Client } = {}
) {
  const queryKey = listThreadsSuspenseQueryKey(params)
  return queryOptions<
    ListThreadsQueryResponse,
    ResponseErrorConfig<ListThreads422>,
    ListThreadsQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return listThreads(params, {
        ...config,
        signal: config.signal ?? signal,
      })
    },
  })
}

/**
 * @description Conversations the reader is in, most recently spoken in first.
 * @summary List Threads
 * {@link /api/v1/messages}
 */
export function useListThreadsSuspense<
  TData = ListThreadsQueryResponse,
  TQueryKey extends QueryKey = ListThreadsSuspenseQueryKey,
>(
  params?: ListThreadsQueryParams,
  options: {
    query?: Partial<
      UseSuspenseQueryOptions<
        ListThreadsQueryResponse,
        ResponseErrorConfig<ListThreads422>,
        TData,
        TQueryKey
      >
    > & { client?: QueryClient }
    client?: Partial<RequestConfig> & { client?: Client }
  } = {}
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {}
  const { client: queryClient, ...resolvedOptions } = queryConfig
  const queryKey =
    resolvedOptions?.queryKey ?? listThreadsSuspenseQueryKey(params)

  const query = useSuspenseQuery(
    {
      ...listThreadsSuspenseQueryOptions(params, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as UseSuspenseQueryOptions,
    queryClient
  ) as UseSuspenseQueryResult<TData, ResponseErrorConfig<ListThreads422>> & {
    queryKey: TQueryKey
  }

  query.queryKey = queryKey as TQueryKey

  return query
}
