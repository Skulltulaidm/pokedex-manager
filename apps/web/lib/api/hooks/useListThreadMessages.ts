/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  ListThreadMessagesQueryResponse,
  ListThreadMessagesPathParams,
  ListThreadMessagesQueryParams,
  ListThreadMessages422,
} from "../types/ListThreadMessages"
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@kubb/plugin-client/clients/axios"
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query"
import { listThreadMessages } from "../clients/listThreadMessages"
import { queryOptions, useQuery } from "@tanstack/react-query"

export const listThreadMessagesQueryKey = (
  thread_id: ListThreadMessagesPathParams["thread_id"] | undefined,
  params?: ListThreadMessagesQueryParams
) =>
  [
    {
      url: "/api/v1/messages/:thread_id/messages",
      params: { thread_id: thread_id },
    },
    ...(params ? [params] : []),
  ] as const

export type ListThreadMessagesQueryKey = ReturnType<
  typeof listThreadMessagesQueryKey
>

export function listThreadMessagesQueryOptions(
  thread_id: ListThreadMessagesPathParams["thread_id"] | undefined,
  params?: ListThreadMessagesQueryParams,
  config: Partial<RequestConfig> & { client?: Client } = {}
) {
  const queryKey = listThreadMessagesQueryKey(thread_id, params)
  return queryOptions<
    ListThreadMessagesQueryResponse,
    ResponseErrorConfig<ListThreadMessages422>,
    ListThreadMessagesQueryResponse,
    typeof queryKey
  >({
    enabled: !!thread_id,
    queryKey,
    queryFn: async ({ signal }) => {
      return listThreadMessages(thread_id!, params, {
        ...config,
        signal: config.signal ?? signal,
      })
    },
  })
}

/**
 * @description What was said, newest page first. Only the two participants may read it.
 * @summary List Thread Messages
 * {@link /api/v1/messages/:thread_id/messages}
 */
export function useListThreadMessages<
  TData = ListThreadMessagesQueryResponse,
  TQueryData = ListThreadMessagesQueryResponse,
  TQueryKey extends QueryKey = ListThreadMessagesQueryKey,
>(
  thread_id: ListThreadMessagesPathParams["thread_id"] | undefined,
  params?: ListThreadMessagesQueryParams,
  options: {
    query?: Partial<
      QueryObserverOptions<
        ListThreadMessagesQueryResponse,
        ResponseErrorConfig<ListThreadMessages422>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient }
    client?: Partial<RequestConfig> & { client?: Client }
  } = {}
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {}
  const { client: queryClient, ...resolvedOptions } = queryConfig
  const queryKey =
    resolvedOptions?.queryKey ?? listThreadMessagesQueryKey(thread_id, params)

  const query = useQuery(
    {
      ...listThreadMessagesQueryOptions(thread_id, params, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient
  ) as UseQueryResult<TData, ResponseErrorConfig<ListThreadMessages422>> & {
    queryKey: TQueryKey
  }

  query.queryKey = queryKey as TQueryKey

  return query
}
