{
  "properties": {
    "total_cards": { "type": "integer", "title": "Total Cards" },
    "owned_cards": { "type": "integer", "title": "Owned Cards" },
    "catalog_value": {
      "type": "string",
      "pattern": "^(?!^[-+.]*$)[+-]?0*\\d*\\.?\\d*$",
      "title": "Catalog Value"
    },
    "owned_value": {
      "type": "string",
      "pattern": "^(?!^[-+.]*$)[+-]?0*\\d*\\.?\\d*$",
      "title": "Owned Value"
    },
    "missing_value": {
      "type": "string",
      "pattern": "^(?!^[-+.]*$)[+-]?0*\\d*\\.?\\d*$",
      "title": "Missing Value"
    },
    "types": {
      "items": {
        "properties": {
          "type": { "type": "string", "title": "Type" },
          "total": { "type": "integer", "title": "Total" },
          "owned": { "type": "integer", "title": "Owned" }
        },
        "type": "object",
        "required": ["type", "total", "owned"],
        "title": "MarketTypeCount",
        "description": "How many catalog cards carry a type, and how many of those are held.",
        "x-readme-ref-name": "MarketTypeCount"
      },
      "type": "array",
      "title": "Types"
    },
    "change": {
      "anyOf": [
        {
          "properties": {
            "since": { "type": "string", "format": "date", "title": "Since" },
            "from_value": {
              "type": "string",
              "pattern": "^(?!^[-+.]*$)[+-]?0*\\d*\\.?\\d*$",
              "title": "From Value"
            },
            "to_value": {
              "type": "string",
              "pattern": "^(?!^[-+.]*$)[+-]?0*\\d*\\.?\\d*$",
              "title": "To Value"
            },
            "absolute": {
              "type": "string",
              "pattern": "^(?!^[-+.]*$)[+-]?0*\\d*\\.?\\d*$",
              "title": "Absolute"
            },
            "percent": { "type": "number", "title": "Percent" }
          },
          "type": "object",
          "required": [
            "since",
            "from_value",
            "to_value",
            "absolute",
            "percent"
          ],
          "title": "PriceChange",
          "description": "Movement between the oldest reading inside a window and today.",
          "x-readme-ref-name": "PriceChange"
        },
        { "type": "null" }
      ]
    },
    "performance": {
      "anyOf": [
        {
          "properties": {
            "cost_basis": {
              "type": "string",
              "pattern": "^(?!^[-+.]*$)[+-]?0*\\d*\\.?\\d*$",
              "title": "Cost Basis"
            },
            "market_value": {
              "type": "string",
              "pattern": "^(?!^[-+.]*$)[+-]?0*\\d*\\.?\\d*$",
              "title": "Market Value"
            },
            "absolute": {
              "type": "string",
              "pattern": "^(?!^[-+.]*$)[+-]?0*\\d*\\.?\\d*$",
              "title": "Absolute"
            },
            "percent": { "type": "number", "title": "Percent" },
            "positions": { "type": "integer", "title": "Positions" },
            "positions_without_cost": {
              "type": "integer",
              "title": "Positions Without Cost"
            }
          },
          "type": "object",
          "required": [
            "cost_basis",
            "market_value",
            "absolute",
            "percent",
            "positions",
            "positions_without_cost"
          ],
          "title": "PortfolioReturn",
          "description": "What the holdings cost against what they are worth now.\n\nMeasured only over the copies with a recorded cost, and it says how many\nthose are: a return computed over half a collection would otherwise read as\na return over all of it.",
          "x-readme-ref-name": "PortfolioReturn"
        },
        { "type": "null" }
      ]
    }
  },
  "type": "object",
  "required": [
    "total_cards",
    "owned_cards",
    "catalog_value",
    "owned_value",
    "missing_value",
    "types"
  ],
  "title": "MarketSummary",
  "x-readme-ref-name": "MarketSummary"
}
