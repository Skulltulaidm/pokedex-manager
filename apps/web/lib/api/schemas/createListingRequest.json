{
  "properties": {
    "give": {
      "items": {
        "properties": {
          "card_id": { "type": "string", "title": "Card Id" },
          "condition": {
            "anyOf": [
              {
                "type": "string",
                "enum": [
                  "mint",
                  "near_mint",
                  "lightly_played",
                  "moderately_played",
                  "heavily_played",
                  "damaged"
                ],
                "title": "CardCondition",
                "x-readme-ref-name": "CardCondition"
              },
              { "type": "null" }
            ]
          }
        },
        "type": "object",
        "required": ["card_id"],
        "title": "OfferCardInput",
        "description": "A card put on the table, in a state.\n\nLeaving the condition out lets the service pick the worst copy its owner\nholds, which is the one a collector parts with.",
        "x-readme-ref-name": "OfferCardInput"
      },
      "type": "array",
      "minItems": 1,
      "title": "Give"
    },
    "want": {
      "items": { "type": "string" },
      "type": "array",
      "minItems": 1,
      "title": "Want"
    },
    "note": {
      "anyOf": [{ "type": "string", "maxLength": 280 }, { "type": "null" }],
      "title": "Note"
    }
  },
  "type": "object",
  "required": ["give", "want"],
  "title": "CreateListingRequest",
  "description": "Wanted cards are ids alone: a state can only be promised by whoever holds\nthe card, and on this side nobody holds it yet.",
  "x-readme-ref-name": "CreateListingRequest"
}
