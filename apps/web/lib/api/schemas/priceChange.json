{
  "properties": {
    "since": { "type": "string", "format": "date", "title": "Since" },
    "from_value": {
      "type": "string",
      "pattern": "^(?!^[-+.]*$)[+-]?0*\\d*\\.?\\d*$",
      "title": "From Value"
    },
    "to_value": {
      "type": "string",
      "pattern": "^(?!^[-+.]*$)[+-]?0*\\d*\\.?\\d*$",
      "title": "To Value"
    },
    "absolute": {
      "type": "string",
      "pattern": "^(?!^[-+.]*$)[+-]?0*\\d*\\.?\\d*$",
      "title": "Absolute"
    },
    "percent": { "type": "number", "title": "Percent" }
  },
  "type": "object",
  "required": ["since", "from_value", "to_value", "absolute", "percent"],
  "title": "PriceChange",
  "description": "Movement between the oldest reading inside a window and today.",
  "x-readme-ref-name": "PriceChange"
}
