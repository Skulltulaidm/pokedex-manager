/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import { concentrationBucketSchema } from "./concentrationBucketSchema"
import { z } from "zod/v4"

/**
 * @description How few cards carry the value.\n\n`cards_for_half` is the count of positions, largest first, that reach half\nthe portfolio: one number for how exposed the whole thing is to a handful of\ncards. Unpriced holdings are counted but never valued.
 */
export const portfolioConcentrationSchema = z
  .object({
    total_value: z.string().regex(/^(?!^[-+.]*$)[+-]?0*\d*\.?\d*$/),
    priced_positions: z.int(),
    unpriced_positions: z.int(),
    get buckets() {
      return z.array(
        concentrationBucketSchema.describe(
          "What the largest `cards` positions add up to."
        )
      )
    },
    cards_for_half: z.union([z.int(), z.null()]),
  })
  .describe(
    "How few cards carry the value.\n\n`cards_for_half` is the count of positions, largest first, that reach half\nthe portfolio: one number for how exposed the whole thing is to a handful of\ncards. Unpriced holdings are counted but never valued."
  )
