/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import { cardViewSchema } from "./cardViewSchema"
import { z } from "zod/v4"

/**
 * @description A catalog card plus how many copies the reader holds. Zero is a real\nanswer here, not a missing one: the whole grid is owned against unowned.
 */
export const marketCardViewSchema = z
  .object({
    get card() {
      return cardViewSchema.describe(
        "A card with both layers: what is printed, and the species it depicts."
      )
    },
    owned: z.int(),
    item_id: z.optional(z.union([z.uuid(), z.null()])),
  })
  .describe(
    "A catalog card plus how many copies the reader holds. Zero is a real\nanswer here, not a missing one: the whole grid is owned against unowned."
  )
