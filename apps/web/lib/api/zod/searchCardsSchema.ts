/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import { HTTPValidationErrorSchema } from "./HTTPValidationErrorSchema"
import { cardViewSchema } from "./cardViewSchema"
import { z } from "zod/v4"

export const searchCardsQueryParamsSchema = z.object({
  q: z.optional(z.union([z.string(), z.null()])),
  set_id: z.optional(z.union([z.string(), z.null()])),
  limit: z.coerce.number().int().min(1).max(100).default(30),
})

/**
 * @description Successful Response
 */
export const searchCards200Schema = z.array(
  z
    .lazy(() => cardViewSchema)
    .describe(
      "A card with both layers: what is printed, and the species it depicts."
    )
)

/**
 * @description Validation Error
 */
export const searchCards422Schema = z.lazy(() => HTTPValidationErrorSchema)

export const searchCardsQueryResponseSchema = z.lazy(() => searchCards200Schema)
