/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@kubb/plugin-client/clients/fetch"
import type {
  CreateScanMutationRequest,
  CreateScanMutationResponse,
  CreateScan422,
} from "../types/CreateScan"
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@kubb/plugin-client/clients/fetch"
import { buildFormData } from "../.kubb/config"

function getCreateScanUrl() {
  const res = { method: "POST", url: `/api/v1/scans` as const }
  return res
}

/**
 * @description Read a card photo and propose which card it is.
 * Reading and identifying are separate steps on purpose: the model transcribes
 * what is printed, and the lookup decides which card those glyphs belong to.
 * @summary Create Scan
 * {@link /api/v1/scans}
 */
export async function createScan(
  data: CreateScanMutationRequest,
  config: Partial<RequestConfig<CreateScanMutationRequest>> & {
    client?: Client
  } = {}
) {
  const { client: request = fetch, ...requestConfig } = config

  const requestData = data
  const formData = buildFormData(requestData)
  const res = await request<
    CreateScanMutationResponse,
    ResponseErrorConfig<CreateScan422>,
    CreateScanMutationRequest
  >({
    method: "POST",
    url: getCreateScanUrl().url.toString(),
    data: formData as FormData,
    ...requestConfig,
  })
  return res.data
}
