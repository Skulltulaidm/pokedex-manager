/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@kubb/plugin-client/clients/fetch"
import type {
  CardMarketContextQueryResponse,
  CardMarketContextPathParams,
  CardMarketContext422,
} from "../types/CardMarketContext"
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@kubb/plugin-client/clients/fetch"

function getCardMarketContextUrl(
  card_id: CardMarketContextPathParams["card_id"]
) {
  const res = {
    method: "GET",
    url: `/api/v1/catalog/cards/${card_id}/context` as const,
  }
  return res
}

/**
 * @summary Card Market Context
 * {@link /api/v1/catalog/cards/:card_id/context}
 */
export async function cardMarketContext(
  card_id: CardMarketContextPathParams["card_id"],
  config: Partial<RequestConfig> & { client?: Client } = {}
) {
  const { client: request = fetch, ...requestConfig } = config

  const res = await request<
    CardMarketContextQueryResponse,
    ResponseErrorConfig<CardMarketContext422>,
    unknown
  >({
    method: "GET",
    url: getCardMarketContextUrl(card_id).url.toString(),
    ...requestConfig,
  })
  return res.data
}
