/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@kubb/plugin-client/clients/fetch"
import type {
  GetConversationQueryResponse,
  GetConversationPathParams,
  GetConversation422,
} from "../types/GetConversation"
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@kubb/plugin-client/clients/fetch"

function getGetConversationUrl(
  conversation_id: GetConversationPathParams["conversation_id"]
) {
  const res = { method: "GET", url: `/api/v1/chat/${conversation_id}` as const }
  return res
}

/**
 * @summary Get Conversation
 * {@link /api/v1/chat/:conversation_id}
 */
export async function getConversation(
  conversation_id: GetConversationPathParams["conversation_id"],
  config: Partial<RequestConfig> & { client?: Client } = {}
) {
  const { client: request = fetch, ...requestConfig } = config

  const res = await request<
    GetConversationQueryResponse,
    ResponseErrorConfig<GetConversation422>,
    unknown
  >({
    method: "GET",
    url: getGetConversationUrl(conversation_id).url.toString(),
    ...requestConfig,
  })
  return res.data
}
